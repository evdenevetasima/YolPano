services:
  - type: web
    name: yolpano
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /api/stats
    envVars:
      - key: YOLPANO_TICK
        value: "45000"
